#ifndef STRUCTURE
    #include "structure.h"
    #define STRUCTURE
#endif

int multiplication(number_t *num1, number_t *num2, number_t *result);
void round_number(number_t *num, int end, int flag);

int char_to_int(char a);